{
  "name": "asha-assist",
  "version": "1.0.0",
  "description": "Digital platform connecting ASHA workers with families for healthcare services",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && python app.py",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["healthcare", "asha", "community", "react", "flask"],
  "author": "AshaAssist Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}